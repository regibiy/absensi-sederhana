<?php 
    $hostname = "localhost";
    $username = "root";
    $password = "";
    $database = "absensi";

    $db = new mysqli($hostname, $username, $password, $database);
    // -> artinya masuk ke dalam karakteristik objek
?>